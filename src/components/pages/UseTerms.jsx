import { Typography } from "@mui/material"
import { BaseLayout } from "../layouts"


export const UseTerms = () => {
  return(
  <BaseLayout>
  <Typography variant="h4" sx={{mt:12, textAlign:"center"}}>利用規約</Typography>
  </BaseLayout>
  )
}
